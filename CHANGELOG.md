# 변경 이력

이 프로젝트에 추가된 기능과 수정한 버그를 기록합니다. 최신 항목이 위에 옵니다.

---

## 2026-07-26

### 추가: 접수 마감 행사 자동 정리

접수 기간이 끝난 행사를 이미 보낸 Discord 메시지에서 자동으로 제거합니다.

- 마감일은 `접수` 기간의 끝, 없으면 `일시`의 끝에서 추출합니다.
- 원문에 연도가 없어 섹션 제목(`26년 07월`)의 연도를 빌려오고, `12월 ~ 01월`처럼 해를 넘기는 구간은 +1년 보정합니다.
- 마감일 당일까지는 유지하고, 다음 날 실행부터 정리합니다.
- 메시지에 유효한 행사가 남아 있으면 그 행사들만으로 메시지를 수정(`PATCH`), 전부 마감이면 메시지를 삭제(`DELETE`)합니다.
- 봇 계정 없이 웹훅 API만으로 동작합니다. 대신 전송 시 `?wait=true`를 붙여 메시지 ID를 받아 캐시에 저장해야 합니다.
- 마감일을 판별할 수 없는 행사는 정보 손실을 피하기 위해 그대로 둡니다.

캐시 형식을 v3로 올려 `deadline`과 `messages`(웹훅별 메시지 ID) 필드를 추가했습니다.

> 제약: 이 기능 배포 이전에 보낸 메시지는 ID가 캐시에 없어 정리할 수 없습니다. 배포 후 새로 보내는 메시지부터 적용됩니다.

### 수정: 메타데이터에 섞인 HTML 태그가 알림에 노출

Dev-Event README에 `접수: 05. 11(월) ~ 08. 31(월) <br />`처럼 HTML 태그가 섞인 항목이 있어, 알림 요약에 `<br />`가 그대로 찍혔습니다. `MarkdownParser._normalize_text`에서 태그를 제거하도록 수정했습니다.

### 추가: 컴팩트 다이제스트 (기본 전송 방식)

"채널에 알림이 너무 쌓여 정보가 과도하게 노출된다"는 피드백에 대응해, 전체 행사를 임베드 1개의 목록으로 압축합니다.

- 행사당 `제목 링크` + 한 줄 요약(일시 또는 접수기간 · 장소 · 유무료 · 분류 · 주최)
- 분류는 말머리 이모지로 구분 (🔴 대회·해커톤, 🟢 세미나·컨퍼런스, 🟠 교육·부트캠프, 🔵 모임·동아리, ⚪ 기타)
- 임베드 색상은 묶음 내 우선순위가 가장 높은 분류 기준
- 메시지 분할은 건수(20)와 description 문자 예산(3800) 양쪽 기준 — Discord 4096자 제한 대응
- 기존 표현은 `DIGEST_STYLE=rich`로 유지. 워크플로 Variables로 코드 수정 없이 되돌릴 수 있습니다.

### 수정: 제목의 대괄호가 Markdown 링크를 깨뜨림

`[온라인] 7월 바이브 코드 러시`처럼 제목에 대괄호가 들어가면 컴팩트 목록의 `[제목](URL)` 링크가 깨졌습니다. 링크 라벨의 `[`, `]`를 이스케이프하도록 수정했습니다.

### 수정: `datetime.utcnow()` 사용

Python 3.12부터 deprecated이고 향후 제거 예정이라 테스트 실행 시 경고가 출력됐습니다. `datetime.now(timezone.utc)`로 교체했습니다.

---

## 2026-07-19

### 추가: 다이제스트 전송, 분류별 색상, 구조화된 필드

- 새 행사 여러 건을 메시지 1개당 임베드 최대 10개로 묶어 전송 (행사 1건당 메시지 1개 → 알림 횟수 감소)
- 분류 키워드에 따라 임베드 색상 지정
- 분류/주최/접수/시기를 각각 별도 필드로 표시 (기존에는 한 줄 텍스트)

### 추가: 캐시 v2 — 이벤트 객체 저장과 중복 판정 개선

캐시가 URL 문자열 배열이라 URL이 바뀌면 같은 행사도 다시 알림이 갔습니다.

- 전송한 행사를 객체(제목/URL/월/메타데이터/전송일시)로 저장
- URL 정규화 후 비교: 추적 파라미터(`utm_*`, `fbclid` 등)·fragment·끝 슬래시·`www.` 차이를 무시
- 제목+월 병행 판정: URL이 바뀌어 재등록돼도 중복으로 차단
- 구버전(v1) 캐시는 로드 시 자동 마이그레이션, 이후 실행에서 제목/월을 자동 백필

### 추가: 오래된 캐시 자동 정리, DRY RUN 모드

- 현재 월 기준 3개월 이전 행사를 캐시에서 제거 (`RETENTION_MONTHS`로 조정)
- `DRY_RUN=1`로 전송과 캐시 변경 없이 파싱·중복 판정만 검증

### 수정: 워크플로가 v2 캐시를 손상으로 오판

`Initialize git-backed cache` 단계가 캐시 파일이 JSON 배열인지만 검사해서, v2 객체 형식(`{"version": 2, "events": [...]}`)을 만나면 손상으로 판단해 `[]`로 초기화했습니다. 그대로 뒀다면 배포 직후 모든 행사가 신규로 판정돼 한꺼번에 재전송됐을 문제입니다. 배열(v1)과 객체(v2) 양쪽을 허용하도록 수정했습니다.

### 발견: Actions의 캐시 커밋이 조용히 실패하고 있었음

7월 17일에 Discord 알림은 나갔는데 저장소의 `events_cache.json`은 6월 27일 상태 그대로였습니다. `Commit and push git-backed cache` 단계에 `continue-on-error: true`가 걸려 있어 실패해도 워크플로가 초록불로 끝나 눈치채기 어려웠습니다.

배포 시 현재 행사 전체를 전송 없이 캐시에 기록(시딩)해 중복 알림을 막았습니다. 시딩 스크립트는 README의 "캐시 파일 준비" 절에 있습니다.

> 운영 팁: Actions 로그에서 이 단계가 계속 성공하는지 주기적으로 확인하세요. 실패가 누적되면 같은 행사가 다시 전송될 수 있습니다.

---

## 참고: 로컬 개발 중 자주 겪은 문제

코드 버그는 아니지만 반복해서 막혔던 지점들입니다.

| 증상 | 원인 | 해결 |
| --- | --- | --- |
| `zsh: command not found: python` | macOS에는 `python` 명령이 없음 | `python3` 사용, 또는 가상환경 활성화 후 `python` |
| `ModuleNotFoundError: No module named 'requests'` | 가상환경 미활성화 상태로 시스템 인터프리터 실행 | `source .venv/bin/activate` 후 실행 |
| `Start directory is not importable: 'tests'` | 저장소 폴더가 아닌 곳에서 실행 | `cd ~/Desktop/dev-event-bot` 후 실행 |
| `error: could not write index` / `index.lock` | 이전 git 프로세스가 남긴 잠금 파일 | `rm -f .git/index.lock` |
| `! [rejected] main -> main (fetch first)` | Actions가 캐시 커밋을 푸시해 원격이 앞섬 | `git pull --rebase` 후 `git push` |

---

## 테스트 현황

| 파일 | 대상 |
| --- | --- |
| `tests/test_markdown_parser.py` | README Markdown 파싱, HTML 태그 제거 |
| `tests/test_event_cache.py` | 캐시 마이그레이션, URL·제목 정규화, 중복 판정, 오래된 항목 정리 |
| `tests/test_discord_sender.py` | 임베드 생성, 분류별 색상, 다이제스트 분할 |
| `tests/test_expiry_cleanup.py` | 마감일 파싱, 만료 판정, 메시지 수정·삭제 |

```bash
source .venv/bin/activate && python -m unittest discover -s tests
```
